pipeline {
	agent any 
	stages {
		stage("working with variables") {
			steps {
				script {
					var1=10
					var2="Number is 10"
					println " var 1 is ${var1} , and var2 value is ${var2}"
					}
				}
			}
		}
	}
}
